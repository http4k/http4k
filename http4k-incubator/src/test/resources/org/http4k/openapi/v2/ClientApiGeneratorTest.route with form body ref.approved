>>>TitleClient.kt

package testPackage.client

import kotlin.Boolean
import org.http4k.core.Body
import org.http4k.core.Body.webForm
import org.http4k.core.HttpHandler
import org.http4k.core.Method
import org.http4k.core.Request
import org.http4k.core.with
import org.http4k.lens.FormField
import org.http4k.lens.Validator.Strict
import org.http4k.lens.boolean

class TitleClient(
	private val httpHandler: HttpHandler
) {
	fun endpoint(required: Boolean) {

		val requiredLens = FormField.boolean().required("required")
		val formLens = Body.webForm(Strict, requiredLens).toLens()
		val httpReq = Request(Method.POST, "/formreusable")
			.with(formLens of request)

		httpHandler(httpReq)
	}
}
