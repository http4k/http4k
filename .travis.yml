matrix:
  include:
    - language: java
      jdk: oraclejdk8
      script:
      - "./gradlew check"
      after_success:
      - "./gradlew jacocoRootReport coveralls"
      - "travis_wait 60 ./release-ci.sh"
      cache:
        directories:
        - "$HOME/.m2"
        - "$HOME/.gradle"
    - language: python
      script:
        - "./release-site.sh"
group: deprecated-2017Q2
sudo: true