description = 'A set of kotest matchers for common http4k types'

dependencies {
    compile project(":http4k-core")

    compile Libs.kotest

    testCompile project(":http4k-format-jackson")
    testCompile Config.TestDependencies - Libs.hamkrest
}
