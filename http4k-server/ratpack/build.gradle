description = 'Http4k HTTP Server built on top of Ratpack'

dependencies {
    api project(":http4k-core")

    api Libs.ratpack_core

    testCompile project(path: ":http4k-core", configuration: 'testArtifacts')
    testCompile Config.TestDependencies
}
